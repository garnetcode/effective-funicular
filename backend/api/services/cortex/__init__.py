# This package contains the sensory cortex modules for the Chimera agents.
# Each cortex is responsible for processing a specific modality of raw input
# (e.g., images, text, tabular data) and converting it into a fixed-size
# embedding vector for the agent's brain to process.
